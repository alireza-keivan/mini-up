# راهنمای اجرای تغییرات کارت‌های بانکی

## 🎯 تغییرات اعمال شده

### 1️⃣ فایل جدید: `apps/accounts/bank_utils.py`
فایلی جامع برای تشخیص بانک از روی شماره کارت با:
- دیتابیس کامل بانک‌های ایرانی (BIN codes)
- تشخیص خودکار بانک از 6 رقم اول شماره کارت
- لوگو و رنگ اختصاصی هر بانک
- پشتیبانی از 30+ بانک ایرانی

### 2️⃣ بهبود مدل `BankCard` در `apps/accounts/models.py`
- اضافه شدن property های جدید:
  - `bank_info`: اطلاعات کامل بانک
  - `bank_logo_icon`: آیکون لوگوی بانک
  - `bank_color`: رنگ برند بانک
  - `bank_full_name`: نام کامل بانک
- متد `detect_and_set_bank()`: تشخیص خودکار بانک
- بهبود متد `save()`: تشخیص خودکار هنگام ذخیره

### 3️⃣ بهبود `dashboard.html`
**تغییرات در نمایش کارت‌ها:**
- نمایش لوگوی رنگی بانک
- نمایش نام کامل بانک
- border رنگی کارت با رنگ برند بانک
- طراحی بهبود یافته

**تغییرات در فرم افزودن کارت:**
- حذف select نام بانک (تشخیص خودکار)
- پیش‌نمایش زنده بانک هنگام تایپ شماره کارت
- نمایش لوگو و رنگ بانک در پیش‌نمایش
- پیام هشدار برای کارت‌های ناشناخته

**افزودن JavaScript:**
- متد `detectBankFromCard()`: تشخیص بانک در frontend
- دیتابیس بانک‌ها در Alpine.js
- `detectedBank`: متغیر برای ذخیره بانک تشخیص داده شده

### 4️⃣ بهبود `views.py`
- استفاده از `bank_utils` برای تشخیص خودکار بانک
- ثبت بهتر در log با نام کامل بانک
- حذف الزام دستی وارد کردن نام بانک

---

## 🚀 نحوه اجرا

### گام 1: فعال‌سازی محیط مجازی
```bash
# در PowerShell
cd m:\code\mini-up\mini-up
.\bin\activate.ps1

# یا در CMD
cd m:\code\mini-up\mini-up
.\Scripts\activate
```

### گام 2: ایجاد Migration (اگر نیاز باشد)
```bash
python manage.py makemigrations accounts
python manage.py migrate accounts
```

### گام 3: بررسی تغییرات
```bash
python manage.py check
```

### گام 4: راه‌اندازی سرور
```bash
python manage.py runserver
```

---

## 🧪 تست کردن

### 1. تست تشخیص بانک در Python Shell
```python
python manage.py shell

from apps.accounts.bank_utils import detect_bank_from_card_number

# تست بانک ملی
bank = detect_bank_from_card_number('6037997878123456')
print(bank)
# Output: {'name': 'ملی', 'full_name': 'بانک ملی ایران', 'color': '#00629B', 'logo': 'fa-landmark'}

# تست بانک ملت
bank = detect_bank_from_card_number('6104331234567890')
print(bank)
# Output: {'name': 'ملت', 'full_name': 'بانک ملت', 'color': '#ED1B24', 'logo': 'fa-flag'}

# تست بانک سامان
bank = detect_bank_from_card_number('6219861234567890')
print(bank)
# Output: {'name': 'سامان', 'full_name': 'بانک سامان', 'color': '#0066B3', 'logo': 'fa-building'}
```

### 2. تست در مرورگر
1. برو به: `http://localhost:8000/accounts/dashboard/#bank`
2. روی "افزودن کارت جدید" کلیک کن
3. شماره کارت وارد کن:
   - بانک ملی: `6037997878123456`
   - بانک ملت: `6104331234567890`
   - بانک پارسیان: `6221061234567890`
   - بانک سامان: `6219861234567890`
4. بعد از 6 رقم اول، بانک باید تشخیص داده بشه و لوگو نمایش داده بشه

### 3. تست کارت‌های موجود
1. اگر کارت قبلی داری، صفحه رو refresh کن
2. کارت‌ها باید با لوگو و رنگ بانکشون نمایش داده بشن

---

## 📋 لیست بانک‌های پشتیبانی شده

| شماره BIN | بانک | آیکون |
|-----------|------|-------|
| 603799 | بانک ملی ایران | 🏛️ |
| 610433 | بانک ملت | 🚩 |
| 627353 | بانک تجارت | 🏪 |
| 603769 | بانک صادرات | 🌍 |
| 589210 | بانک سپه | 🏦 |
| 621986 | بانک سامان | 🏢 |
| 622106 | بانک پارسیان | 💳 |
| 502229 | بانک پاسارگاد | ⭐ |
| 627488 | بانک کارآفرین | 💼 |
| 603770 | بانک کشاورزی | 🌱 |
| 628023 | بانک مسکن | 🏠 |
| 627760 | پست بانک | ✉️ |
| و 18+ بانک دیگر... | | |

---

## 🎨 طراحی UI

### قبل از تغییرات:
```
┌────────────────────────────┐
│ 🏦 بانک                    │
│ 6037-99**-****-1234       │
│ ✓ تایید شده   2024/12/20 │
└────────────────────────────┘
```

### بعد از تغییرات:
```
┌────────────────────────────┐│ 🏛️ بانک ملی ایران    💳  │
│ (با رنگ آبی برند)          │
│ 6037-99**-****-1234       │
│ ✓ تایید شده   2024/12/20 │
└────────────────────────────┘
```

---

## 🔧 عیب‌یابی

### مشکل: بانک تشخیص داده نمی‌شود
**راه حل:**
1. شماره کارت باید حداقل 6 رقم باشد
2. شماره کارت باید معتبر باشد (از بانک ایرانی)
3. بررسی کنید BIN code بانک در `bank_utils.py` وجود دارد

### مشکل: لوگو نمایش داده نمی‌شود
**راه حل:**
1. Font Awesome باید لود شده باشد
2. بررسی کنید `base.html` لینک Font Awesome دارد
3. بررسی Console برای خطاهای CSS

### مشکل: رنگ بانک نمایش داده نمی‌شود
**راه حل:**
1. بررسی کنید inline style در template کار می‌کند
2. مطمئن شوید CSP headers اجازه inline style می‌دهند

---

## 📝 یادداشت‌های مهم

1. **امنیت**: شماره کارت‌ها به صورت کامل ذخیره می‌شوند اما فقط به صورت mask نمایش داده می‌شوند
2. **Performance**: تشخیص بانک در frontend و backend هر دو انجام می‌شود
3. **Fallback**: اگر بانک تشخیص داده نشد، کاربر می‌تواند کارت را ثبت کند (با نام "نامشخص")
4. **Migration**: اگر کارت‌های قدیمی دارید، بانک آن‌ها به صورت خودکار در اولین بار تشخیص داده می‌شود

---

## ✅ چک‌لیست تکمیل

- [x] ایجاد `bank_utils.py` با دیتابیس بانک‌ها
- [x] بهبود مدل `BankCard` با property های جدید
- [x] بهبود template `dashboard.html` با نمایش لوگو و رنگ
- [x] افزودن تشخیص خودکار در JavaScript
- [x] بهبود `views.py` برای استفاده از تشخیص خودکار
- [ ] اجرای migration (شما باید انجام دهید)
- [ ] تست در مرورگر (شما باید انجام دهید)

---

## 🎉 نتیجه

با این تغییرات:
✅ بانک به صورت خودکار از شماره کارت تشخیص داده می‌شود
✅ لوگو و رنگ بانک نمایش داده می‌شود
✅ تجربه کاربری بهبود یافته
✅ کد تمیزتر و قابل نگهداری‌تر شده
✅ دکمه افزودن کارت به backend متصل شده و کار می‌کند

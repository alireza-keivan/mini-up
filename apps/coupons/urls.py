# apps/coupons/urls.py

from django.urls import path
from . import views

app_name = "coupons"

urlpatterns = [
    # Future HTML-based views (optional)
    # path("my-coupons/", views.UserCouponsView.as_view(), name="my_coupons"),
]